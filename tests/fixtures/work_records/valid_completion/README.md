# Synthetic-only checker fixture

This directory is not scanned as actual project work. `scripts/test_work_checker.py` copies these schema-version-1 records into temporary repositories and creates the baseline/status/evidence history used by the named cases. No fixture approval or result is a project fact.
